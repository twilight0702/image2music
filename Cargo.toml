[package]
name = "image2music"
version = "0.1.0"
edition = "2021"
description = "Convert the visual structure of PNG and JPEG images into WAV music"
license = "MIT"

[dependencies]
clap = { version = "4.5", features = ["derive"] }
hound = "3.5"
image = "0.25"

[dev-dependencies]
tempfile = "3.13"
